"""兴趣图谱相关提示词:实体关系抽取 / 补漏 / 实体消解 / 社区摘要。"""
from .ontology import ENTITY_TYPES, RELATION_TYPES

VERSION = "interest_v3"

# 受控词表与规模上限(抽取与归一化共用,单一来源)
MAX_ENTITIES = 10
MAX_RELATIONS = 10
ENTITY_TYPE_TEXT = "/".join(ENTITY_TYPES)
RELATION_TYPE_TEXT = "/".join(RELATION_TYPES)
# 兼容抽取提示词里的占位符写法(原文保留,不改提示词一字)
_ENTITY_TYPE_TEXT = ENTITY_TYPE_TEXT
_RELATION_TYPE_TEXT = RELATION_TYPE_TEXT

EXTRACT_SYSTEM_PROMPT = f"""你是知识图谱抽取器。从用户这句话里抽取「实体」与「实体之间的关系」,并标注用户是否在长期关注这些实体。

实体规则:
- 所有出现的具体对象/专有名词/领域名都抽成实体(如 NBA、CBA、库里、篮球、手冲咖啡、V60)。
- 只抽名词性对象:人物/球队/联赛/运动项目/作品/产品/技术/地点/组织;不要抽身体部位、动作、
  抽象过程、学历阶段和泛指词(例如"膝盖""读研""检索""时间""事情"都不是实体)。
- type 只能是:{_ENTITY_TYPE_TEXT}(league=联赛/赛事,team=球队,sport=运动项目,work=作品,product=产品)。
- category 只能是:体育/科技/影视/音乐/美食/游戏/学术/旅行/其他。
- aliases 是同一实体的其他叫法(原句没有也可以给同义简称,如 美职篮)。
- description 一句话说明这是什么。
- is_followed=true:用户长期关注或持续参与的对象——领域/品类/运动项目/联赛/球队/正在学的技术/
  正在玩的游戏/习惯性在喝的饮品(如 篮球、NBA、手冲咖啡、大模型应用、独立游戏、半程马拉松)。
- is_followed=false:只是被顺带提到的道具、人物或单次对象(如 V60 滤杯、库里、某次买的豆子);
  这些留作实体节点用于关系即可。
- **同一概念必须复用已知名称**:已知实体里有 "RAG" 时,句子里说的"检索项目/检索系统"要用 "RAG";
  有 "大模型" 时,说"大模型应用/LLM"要用 "大模型";有 "手冲咖啡" 时,说"手冲"要用 "手冲咖啡"。
- 不能凭空补实体:句子没提到、也不是用户已有实体的,绝对不要输出。
- 每句最多 {MAX_ENTITIES} 个实体。

关系规则:
- 只抽两端都出现在本次实体列表里的关系。
- type 只能是:{_RELATION_TYPE_TEXT}(broader=专指→泛指,member_of=成员→组织,part_of=部分→整体)。
- 每句最多 {MAX_RELATIONS} 条关系。

证据规则(最重要):
- 每个实体和每条关系都必须给 evidence:必须是**用户原句里的连续片段,原样摘抄**,不得改写、拼接或翻译。
- 原句里找不到对应片段的,不要输出该实体/关系。

时间规则:
- since/until 用 YYYY-MM 或 YYYY-MM-DD;不确定填 null。
- "最近/这阵子"等相对时间无法确定时填 null(系统会兜底)。
- "不看了/弃了/不追了"等结束语义放进 until。

只输出一个 JSON 对象,不要解释、不要 markdown 包裹。格式:
{{"entities":[{{"name":"NBA","type":"league","category":"体育","aliases":["美职篮"],"description":"美国职业篮球联赛","is_followed":true,"since":"2024-11","until":null,"confidence":0.9,"evidence":"我最近迷上NBA了"}}],
  "relations":[{{"from":"NBA","to":"篮球","type":"broader","evidence":"NBA"}}]}}
没有可抽内容时返回 {{"entities":[],"relations":[]}}。
"""

EXTRACT_GLEAN_PROMPT = (
    "可能还有遗漏的实体或关系。请只补充**新增**的实体与关系(格式相同,JSON),"
    '已经给过的不要重复;确实没有遗漏就返回 {"entities":[],"relations":[]}。'
)

ENTITY_JUDGE_PROMPT = (
    "你是知识图谱的实体消解模块。给定两个实体名称,判断它们是否指同一个事物"
    "(同一对象的不同说法算同一个,例如'检索项目'与'RAG检索'、'V60滤杯'与'V60';"
    "上下位关系不算,例如'科幻电影'与'电影'不是同一个)。只回答 yes 或 no。"
)

DEDUP_AUDIT_PROMPT = (
    "你是知识图谱的实体消解模块。下面是一张实体名称列表,请找出其中**指同一个事物**的名字"
    "(同一对象的不同说法:简称/全称/别称,例如 RAG 与 检索项目、大模型 与 大模型应用)。"
    "注意:**上下位关系不算重复**(例如 电影/科幻电影、游戏/独立游戏、咖啡/手冲咖啡 都不是同一实体)。"
    "**层级/组成部分/衍生对象也不算重复**(例如 Rust 与 tokio、NBA 与 勇士队、手冲咖啡 与 V60 滤杯)。"
    '只输出 JSON:{"groups":[["名字1","名字2"]]};没有重复就输出 {"groups":[]}。'
)

COMMUNITY_SUMMARY_PROMPT = (
    "你是个人兴趣画像的社区摘要器。下面是某用户兴趣图谱里的一组实体(同一主题簇)及其内部关系,"
    "以及每个实体的类型与关注时间。请输出 JSON:"
    '{"name":"给这个兴趣簇起一个 4~10 字的中文名字(如 篮球/体育线、手冲咖啡线、大模型技术线)",'
    '"summary":"2~3 句面向用户本人的概述:这组兴趣是什么、彼此怎么关联、时间上有什么特点"}。'
    "只输出 JSON。"
)


# 批量输入(一次抽取多条消息)时追加的说明:行首 [时间] 只是标记,不是用户原话
BATCH_INSTRUCTION = (
    "\n\n# 批量对话输入\n"
    "下面每一行是用户在不同时间说的一句话,行首的 [时间] 只是标记,不是用户原话的一部分。\n"
    "请为**每一行分别**抽取实体与关系;evidence 必须是该行去掉 [时间] 之后的连续片段,\n"
    "不要把 [时间] 标记写进 evidence,也不要把不同行的内容拼在一起作为证据。"
)
